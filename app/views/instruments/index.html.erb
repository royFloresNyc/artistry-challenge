<h1>Instruments</h1>
<ul>
    <% @instruments.each do |instrument| %>
        <li>
            <%= link_to instrument.name, instrument_path(instrument) %> -
            <em><%= instrument.classification %></em>
        </li>
    <% end %>
</ul>